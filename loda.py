# Test comment added at 1761959335
# Welcome to the most broken program ever

import mathh

pritn("Starting Program..." # missing parenthesis

name = input("What is your name?"    # missing closing parenthesis, indentation errors coming!!

if name = "Bob":
print("Hey Bob!"
else
    print("Hello " + name

numbers = [10, 5, 0, 8]
total = 0

for n in numbers
    total += numbers[n]  # using number as index incorrectly

print("Total is: " + total)  # string + int error

def divide(a, b):
return a / b   # indentation error

print(divide(10, 0))  # division by zero

d = {}
d["age"] = 20
print(d["name"])  # key doesn't exist

class Person
    def __init__(self, name, age)
        self.name = nae   # variable typo
        self.age = age

    def speak(self)
        print("Hi I'm " + self.name + " and I am " + self.age + " years old")  # str + int error

p = Person(name, "twenty")  # wrong constructor usage
p.speek()  # wrong method name

print("Program done!"
